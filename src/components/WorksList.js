import React from 'react'

const WorksList = (props)=>{
  let data = props.data;
    console.log(data);
    // <ul>
    //     { props.list.map((work, index) => (
    //     <li key={index}>
    //       <div>
    //         <h4>{work.title}</h4>
    //         <span>주제 : {work.theme}</span>
    //         <span>구분 : {work.sort}</span>
    //         <span>언어 : {work.lang}</span>
    //         <a href={work.url}>바로가기</a>
    //       </div>
    //     </li>
    //     )) }
    // </ul>
  //     return (
  // )
}


export default WorksList;
